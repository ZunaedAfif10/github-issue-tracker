1.var is function scoped and it can be redeclared and updated.let is block scoped and it can be updated but it can't be redeclared in the same scope.And const is also block scoped but it can't be updated or redeclared after assigning.

2.The spread operator is used to expand elements of an array or object.

3.map() works by creating new array by transforming elements.filter() works by creating new array with elements that pass a condition.and foreach() loops in an array but does not return a new array.

4.Arrow function is intoduced in ES6 to write function in a shorter syntax.

5.Template literals are string which is written using backticks that allows embedded variables and multi-line text.
